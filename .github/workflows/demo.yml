name: Demo Workflow
on: 
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
jobs:
    my-job:
      runs-on: ubuntu-latest
      steps:
        - name: Probe
          run: echo "This is a demo workflow for testing purposes."
